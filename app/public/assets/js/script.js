const message = 'Hello world' // Try edit me

// Update header text
document.querySelector('#header').innerHTML = message

var elements = document.querySelectorAll('input[id^="test"]');

for (let ndx = 0; ndx < elements.length; ndx++) {
  const item = elements[ndx];
  
}

// Log to console
console.log(message)