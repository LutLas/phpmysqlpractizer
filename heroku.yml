build:
  docker:
    web: Dockerfile
    php:
        build:
            context: .
            dockerfile: PHP.Dockerfile
        volumes:
            - ./app:/app
run:
  web: bundle exec puma -C config/puma.rb